export enum Weather {
    sunny = "soleil",
    rainy = "pluie",
    cloudy = "nuages",
    snowy = "neige",
    windy = "vent",
    foggy = "brouillard",
}

export enum IntersectionType {
    straight = "ligne droite",
    curve = "virage",
    corner = "virage serré",
    cross = "croisement",
    turn = "tournant",
}
